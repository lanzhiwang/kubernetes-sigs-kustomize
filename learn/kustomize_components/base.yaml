apiVersion: v1
data:
  main.conf: '| color=cornflower_blue log_level=info'
kind: ConfigMap
metadata:
  name: conf-g49hg92hgf
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: example
spec:
  template:
    spec:
      containers:
      - image: example:1.0
        name: example
        volumeMounts:
        - mountPath: /etc/config
          name: conf
      volumes:
      - configMap:
          name: conf-g49hg92hgf
        name: conf
